[[repos]]
repo = "local"

[[repos.hooks]]
id = "build-skills"
name = "Build SKILL.md files from templates"
language = "system"
entry = "bash -c 'python3 build.py && git add plugins/*/skills/*/SKILL.md'"
pass_filenames = false
always_run = true
